# Config local branches + collapsible Caddy — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use TDD.

**Goal:** Config-tab local branch rename/delete + collapsible Caddy panels.

**Files:**
- Create `src/lib/project-local-branch-ops.ts` (+ test)
- Create `src/app/api/projects/[id]/local-branches/route.ts`
- Create `src/components/ProjectLocalBranchesEditor.tsx`
- Modify `ProjectRoutingEditor.tsx`, `ProjectCaddyLogsSection.tsx`, `page.tsx`

## Task 1: Lib ops + tests
## Task 2: API route
## Task 3: Branch editor UI + wire Config tab
## Task 4: Collapsible Caddy sections
## Task 5: `./test.sh`
